1. We need to check whether Pipecat would be more efficient than current setup where we have written the code ourselves in Node.js. We may have to change many things because Pipecat is in Python and we have not used Python anywhere.
2. Is it worth checking - Replace Sarvam's Saarika with something that can be hosted locally so that there is no API round trip? Some of the options are Whisper-based Indic ASR model like open-sarika (~1.5GB, small enough to run on a decent laptop/GPU, plausible even on CPU for non-realtime use) or AI4Bharat's own IndicConformer/IndicWav2Vec models.
3. TODO: Run a local spike (separate from this project, e.g. a standalone `D:\Projects\asr-spike` folder, no changes to this repo) benchmarking `faster-whisper` (or similar) on streaming latency and Hinglish accuracy, to validate item 2 above before deciding whether to pursue it. Needs: confirmation to install Python/pip packages + download a ~1-1.5GB model, and either sample Hindi/Hinglish audio clips, TTS-generated test audio, or a latency-only pass with accuracy unverified.
4. In Step 2.5 (chat/search intent classification, currently `gemini-flash-latest` via `/classify-request`), is it better to host a local model to get the intent instead of a round trip to Gemini?
5. In Step 2.5, if the intent is to chat, shouldn't we use a local model to save turnaround time if the question is really simple, and only call a bigger model if the user asks something complicated?
