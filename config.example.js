// Copy this file to config.js and fill in your own values.
// Get your API key from Google Cloud Console — see README.md for the exact setup steps.
// Restrict this key (HTTP referrers + API restrictions) before using it for real.
window.GOOGLE_MAPS_API_KEY = "your-google-maps-api-key-here";

// Base URL of the local backend (server/) that places calls — e.g. your ngrok
// URL during dev, or http://localhost:3001 if testing without a tunnel.
window.BACKEND_URL = "http://localhost:3001";

// Fixed delivery address the call agent gives to shops when asking about delivery.
window.DELIVERY_ADDRESS = "123 Main St, Your City";
